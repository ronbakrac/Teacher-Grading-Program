To run the program, simply run runproject.bat
Alternitvley, you can use the makefile and compile.
(g++ compiler required for both these options)

User login and student data are stored in plain text so you may edit and view directly in the text file if needed. The default files in this repository have sample student and test data along with a default login of "admin" with password of "password"